.wordpress-org
.git
.github
.claude
.distignore
.gitignore
.DS_Store
*.sql
*.tar.gz
*.zip
postcss.config.js
composer.json
composer.lock
CLAUDE.md
README.md
package-lock.json
node_modules

# NOTE: spa/, webpack.config.js and package.json are deliberately shipped.
# readme.txt points reviewers and users at the un-minified React sources and
# the build setup behind assets/admin/build/*.bundle.js, so they have to be in
# the distributed zip.
